= simple_form_for(@course, :html => {:class => "form-horizontal", :style => "margin: 0 auto; width: 40%;"}) do |f|

  = f.error_notification

  .form-input{:width => "70%"}
    = f.input :course_name, :required => true, placeholder: "Course Name Here"

  .form-actions
    = f.button :submit
