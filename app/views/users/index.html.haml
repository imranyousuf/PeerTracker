%center
  %h1 List of Users
.media
  .media-body
    .container
      %table.table.table-hover
        %thead
          %tr
            %th Name 
            %th UID 
            %th Email 
            %th
            %th
            %th

          %tbody
            - @users.each do |user|
              %tr
                %td= user.name
                %td= user.user_id
                %td= user.email
                %td= link_to 'Show', user
                %td= link_to 'Edit', edit_user_path(user)
                %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

  %br
  %br
= link_to 'Add New User(s)', new_user_path

%br
%br
%br
